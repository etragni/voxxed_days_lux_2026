apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# List the actual Kubernetes YAML files that live in your manifests/base/ directory.
# Update these names if your files are named differently!
resources:
  - deployment.yaml